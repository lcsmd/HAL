      
      common file.list, f.control
      common files(50), dicts(50)

      EQU met.fn to 1 liv.fn to 2, sch.fn to 3
      EQU f.meta to files(1), f.live to files(2), f.schema to files(3)
      equ f.met to files(1)
      equ f.liv to files(2)
      equ f.sch to files(3)

      file.list = "meta,live,schema"

