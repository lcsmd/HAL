      FUNCTION RESOLVE.YTVID(YTVID)
      READ YTURLSTRINGS FROM "DICT","YT" "YT.STRINGS" ELSE
         YTURLSTRINGS = ""
      END
      CT=DCOUNT(YTURLSTRINGS,@FM)
      FOR YID.X = 1 TO CT
         IF LEN(YTVID)=11 THEN EXIT
         YTVID=CHANGE(YTVID,YTURLSTRINGS<YID.X>,"")
      NEXT YID.X
      RETURN YTVID

      END
