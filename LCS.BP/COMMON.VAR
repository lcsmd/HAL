COMMON FILE_LIST, F_CONTROL
COMMON FILES(50), DICTS(50)

EQU MET_FN TO 1 ; * Assigning index for metadata file handle
EQU LIV_FN TO 2 ; * Assigning index for live file handle
EQU SCH_FN TO 3 ; * Assigning index for schema file handle

EQU F_META TO FILES(1)
EQU F_LIV TO FILES(2)
EQU F_SCH TO FILES(3)

FILE_LIST = "meta,live,schema"
