   OPEN 'LCS.TXT' TO F.LCS ELSE PRINT 'LCS.TXT';STOP
   
   EQU T.ID T.B<1>, T.DESC TO T.B<2>, START.PG TP T.B<3> END.PG TO T.B<4>, PG.QTY TO T.B<5>, SECTION TO T.B<6>
   
   READ TOC FROM F.LCS, "TOC.TXT"
   TAB=CHAR(9)
   DELIM=TAB
   CT = DCOUNT(TOC, @FM)
   FOR X = 1 TO CT
      TOC = TRIM(TOC, ".")
      TOC = CHANGE(TOC, TAB, @VM)
      PG<T> = TRIM<TOC<X,2>,".","B")
       IF NUM(PG<T>) THEN 
TOP<T> = TRIM<TOC<X,1>,".")
T+


