# PRD — HAL Voice MVP (Rev4)

- Wake word: **HAL** (fallback **computer**).
- Ack tone on detection.
- Utterance capture ends after ~3 s silence.
- WS-only pipeline. 
- TTS: ElevenLabs with pyttsx3 fallback.
- First client: macOS terminal.

Non-goals: multi-persona, web UI, external proxy exposure (for MVP).
