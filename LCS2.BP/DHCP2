open DHCP TO "DHCP" ELSE PRINT "dhcp";stop
select dhcp
ct=0
loop while readnext aa do
     read buff from dhcp,aa then
          dhcp.cmd = 'set-DhcpServerV4Reservation -ScopeId @ScopId -IPAdress @IPAddress -computername @ServerName -clientid @MacAddress -Name @Name -Description "@Desc"'
          dhcp.cmd = change(D



